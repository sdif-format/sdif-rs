[package]
name = "sdif"
version.workspace = true
edition.workspace = true
description = "SDIF format library — Rust implementation"
license.workspace = true

[dependencies]
sha2 = "0.10"
serde_json = "1"

[dev-dependencies]
serde_json = "1"
